title: get scenarigo repository
steps:
- title: GET single aas
  vars:
    aas_id: 1
  protocol: http
  request:
    method: GET
    url: "https://localhost:8081/shells/{{vars.aas_id}}"
  expect:
    code: OK

